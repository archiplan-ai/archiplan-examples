{
  "node_types": [
    {
      "name": "Subservice",
      "definition": "Internal control-plane subservice running as part of chain_router; holds no canonical state outside chain_router's stores; mutations are local but coordinate over region_coordinator for cross-region truths.",
      "layer": "epistatic",
      "aspects": []
    },
    {
      "name": "StateSet",
      "definition": "Local in-process state set or table maintained by a chain_router subservice; canonical only within this region's chain_router instance, not globally.",
      "layer": "epistatic",
      "aspects": []
    }
  ],
  "edge_types": [
    {
      "name": "calls",
      "layer": "epistatic",
      "from_types": [
        "Subservice"
      ],
      "to_types": [
        "Subservice"
      ]
    },
    {
      "name": "writes_to",
      "layer": "epistatic",
      "from_types": [
        "Subservice"
      ],
      "to_types": [
        "StateSet"
      ]
    },
    {
      "name": "reads_from",
      "layer": "epistatic",
      "from_types": [
        "Subservice"
      ],
      "to_types": [
        "StateSet"
      ]
    }
  ],
  "nodes": [
    {
      "id": "sub_pool_fork_partitioner",
      "node_type": "Subservice",
      "definition": "Front door for incoming RPCs into chain_router: reads the request's (chain, fork) tag and routes it to the per-fork sub-pool, refusing requests that name an unknown or quarantined (chain, fork_id) with a documented error code so requests cannot fall through to a default sub-pool. Maintains a routing table keyed by (chain, fork_id) -> sub-pool that is rebuilt whenever pool_registry transitions a sub-pool's state; the routing table carries the hlc_stamp at which it was rebuilt and refuses to dispatch requests whose lookup pre-dates a fork-transition stamp affecting the target sub-pool — instead the request is bounced to be re-routed against the latest table. Routing also consumes the request's cost-class hint (short-RPC tip-sensitive vs long-RPC archive) and routes only to a replica whose replica-class (read from pool_registry's per-(chain, fork_id) shard) includes the request's class. Per-replica fan-out within a sub-pool uses the effective-capacity denominator (count of replicas currently admitted AND not draining AND not in deferred-quarantine AND not tip-stale) propagated from pool_membership_manager rather than raw pool_registry membership.",
      "aspects": []
    },
    {
      "id": "pool_membership_manager",
      "node_type": "Subservice",
      "definition": "Owns admit/evict decisions for chain pool replicas across all (chain, fork_id) sub-pools: replicas are admitted into a sub-pool, transitioned through healthy/draining/evicted/quarantined lifecycle states, and the per-(chain, fork_id) sub-pool membership is the canonical source for which replicas serve traffic in this region. Lifecycle transitions in pool_registry are gated on the replica's current drain_state_log entry: admit requires drain-completed-or-aborted, evict requires drain-completed; concurrent proposals are CAS-ordered through pool_registry per shard so admit-during-drain or evict-after-readmit cannot both commit. Re-admission of a draining replica requires an explicit drain-abort transition in drain_state_log before admit can commit; auto-admit cannot transition a replica with drain_state_log = drain-in-progress without first proposing drain-abort, which surfaces an alert and records attribution. On dispatch the manager re-validates the replica's currently-recorded fork_id against the request's (chain, fork_id) tag at the moment of dispatch (not just at routing-table read time); a mismatch returns a retryable-on-other-replica signal. Refuses any quarantine eviction whose commit would push the (chain, fork_id) sub-pool's healthy-replica count below the documented safe-membership floor; refusal surfaces an alert with the (chain, fork_id) at risk and the candidate replica. Computes the effective-capacity denominator (count of replicas currently admitted AND not draining AND not in deferred-quarantine AND not tip-stale) from per-(chain, fork_id) pool_registry shard summaries on every relevant transition and propagates it to sub_pool_fork_partitioner so per-replica fan-out reflects the actually-serving subset. Exposes a documented per-(chain, fork_id) target-capacity surface; when traffic share for a sub-pool exceeds its target by a documented margin, the manager proposes membership rebalances (admit additional replicas of that fork, drain over-provisioned rare-fork replicas) gated on the safe-membership floor for both source and destination sub-pools, committed via the same CAS path and visible in audit. Records per-replica replica-class (tip-serving / historical-archive / both) in pool_registry; refuses to admit a tip-class request to an archive-only replica and vice versa. Operator-override admission (used during emergency rollover to admit replacement replicas faster than blue/green allows) requires M-of-N operator quorum signatures (configurable threshold) on the admit proposal — signing operators are recorded with the admit commit and visible in audit; the parent-scope credential authorization model (custody, rotation, compromise-revocation) is bubbled. Enforces a documented max-overrides-per-window rate limit on operator-override admissions that bypass the safe-membership floor; approaching the limit surfaces an alert; exceeding it rejects further override admits with a documented error code. Dedupes inbound offboarding signals from region_coordinator by idempotency key (offboarding_id, component_id, attempt_id), meets a documented attestation SLO, and surfaces preservation-blocked terminal states so region_coordinator can record best-effort attestations. Reads pool_registry through the per-(chain, fork_id) shard plus shard-level summaries for cross-shard consumers.",
      "aspects": []
    },
    {
      "id": "drain_coordinator",
      "node_type": "Subservice",
      "definition": "Choreographs pool drains: a drain proposal moves a replica into 'draining' state in pool_registry, refuses new requests routed to it, holds in-flight RPCs (or returns them with a retryable-on-other-replica hint), waits for gateway to re-bind dependent subscriptions via fanout, and only then commits the drain-completed transition that gates eviction. Owns drain_state_log entries: drain-requested, drain-in-progress, drain-completed, drain-aborted, force-completed (with operator attribution), drain-stuck (with rebind-ack-missed reason). Drain-completion is gated on a bounded wait: if gateway-rebind ack is not received within a documented window, drain_coordinator surfaces a drain-stuck alert and either retries the rebind request idempotently or proposes a force-complete transition that itself requires M-of-N-signed operator override so a stuck gateway cannot indefinitely hold drain capacity hostage. Gateway-rebind acknowledgements consumed by drain_coordinator carry an authenticated signature over the cert-bearing gateway-to-chain_router channel and a per-drain nonce (the drain_id from drain_state_log), so a replayed ack from a prior drain or a forged ack cannot complete an active drain. Coordinates with region_coordinator's lifecycle_gate so a drain cannot proceed if it would push a (chain, fork) sub-pool below documented minimum membership. Pool_registry transitions written by drain_coordinator (draining, evicted) participate in the same per-(chain, fork_id) shard CAS line that pool_membership_manager uses, so admit and evict cannot interleave. On offboarding cancellation overlapping a preservation hold, the drain waits for the long-RPC to complete or to time out (capped by the bounded drain-completion wait), surfaces preservation-blocked terminal state per the offboarding-attestation SLO, and dedupes inbound offboarding signals by idempotency key (offboarding_id, component_id, attempt_id).",
      "aspects": []
    },
    {
      "id": "response_canonicalizer",
      "node_type": "Subservice",
      "definition": "Normalizes every JSON-RPC response leaving a chain pool replica into a canonical form keyed by (chain, method, schema_version): strips/sorts undocumented optional fields, coerces types, applies intra-response ordering. The canonicalizer's output is defined by an explicit canonical-bytes spec keyed by (chain, method, schema_version, canonical-bytes-version): canonical-bytes-version is distinct from schema_version and is bumped when byte-level field ordering, numeric type representation, or whitespace policy changes; the canonicalizer refuses to emit bytes for any (chain, method, schema_version) tuple lacking a registered canonical-bytes-version, and a previously-unseen field surface produces an explicit unknown-field signal rather than a silent canonicalization. Canonical-bytes output is validated against the canonical-bytes-version spec by an independent validator path that does not share code or in-process state with the transform path; the validator's verdict (canonical-affirmed vs canonicalizer-fault) is the input that schema_skew_quarantine consumes — a mis-transform produced by the transform path is detected by the validator and surfaced as a canonicalizer-fault alert distinct from a replica-skew alert. Reads from pool_registry are limited to the per-(chain, fork_id) shard summary needed to compute canonical-shape consensus.",
      "aspects": []
    },
    {
      "id": "schema_skew_quarantine",
      "node_type": "Subservice",
      "definition": "Detects pool replicas whose canonicalized response shape diverges from the (chain, method, schema_version, canonical-bytes-version) cohort's pool consensus shape and proposes their transition to 'quarantined' in pool_registry. Divergence detection indexes consensus shape per cohort: a replica emitting under a new (schema_version, canonical-bytes-version) is compared against the new cohort, not the old; a new cohort is bootstrapped from the registered canonical-bytes-version spec rather than from existing pool members so detection works even when only one replica has rolled to the new version. Consumes the canonicalizer's independent validator verdicts; distinguishes a replica-skew verdict (canonicalized bytes diverge from validator-affirmed pool consensus) from a canonicalizer-fault verdict (validator rejects canonicalizer output as non-canonical) and refuses to commit replica quarantines when the prevailing fault is a canonicalizer-fault, surfacing canonicalizer-fault as its own alert. Quarantine proposals are suppressed only when the rotation-tag at observation time was active AND the rotation event-type is one that can legitimately change response shape (chain-pool blue/green or chain-client-version rotation, not cert or origin-endpoint rotation); the rotation-tag's hlc_stamp is stamped on the proposal at observation, the suppression decision is tied to that stamp, and at commit time the stamped tag is re-checked against current region_coordinator state — a tag change that invalidates suppression triggers re-evaluation. A documented per-cohort suppression timeout caps how long a quarantine can be deferred. Enforces a per-(chain, fork_id) max-concurrent-quarantine-commits cap within a documented window; commits beyond the cap are deferred and surfaced as alerts; the cap is at most a documented fraction of sub-pool membership so quarantine cannot push a sub-pool below safe-membership in one window. Maintains quarantine_set keyed by replica_id with the divergence evidence (consensus shape vs observed shape, canonical-bytes-version, observation hlc_stamp, rotation-tag stamp and event-type) for operator review. Reads pool_registry through the per-(chain, fork_id) shard.",
      "aspects": []
    },
    {
      "id": "tip_freshness_tracker",
      "node_type": "Subservice",
      "definition": "Per-pool tip-freshness signal: measures each pool's tip lag against an out-of-band reference (peer-regional pools sourced via region_coordinator, chain peer count / expected slot advance for the chain) and marks a pool 'tip-stale' when its lag exceeds the per-chain freshness budget. Writes tip-stale state into pool_registry as a per-replica freshness flag (per-(chain, fork_id) shard) and surfaces the per-pool aggregate to fork_detection_alerter and to region_coordinator's tip_quorum so tip-stale pools are excluded from canonical-tip quorum. Uses region_coordinator's hybrid logical clock for freshness measurement; on the HLC-degraded marker the tracker freezes the tip-stale flag set in pool_registry — no transitions in or out of tip-stale commit while degraded, existing flags remain conservatively in place, and the tracker surfaces a tip-freshness-suspended alert and refuses fallback to local wall-clock as the freshness reference. When HLC is degraded, the tracker may use chain-derived signals (chain peer count, expected slot advance, peer-regional pool's chain-coord head delta as relayed by region_coordinator on heal) as freshness inputs because they are timestamp-free; flags derived from chain-only signals are tagged hlc-degraded so consumers (region_coordinator's tip_quorum) apply them more conservatively. Tip-divergence quarantine proposals consult the rotation-tag with explicit event-type discrimination — suppressing only on chain-pool blue/green rotation events, not on cert/origin-endpoint or chain-client-version rotations alone — and stamp the rotation-tag's hlc_stamp on each proposal at observation; commit-time re-check forces re-evaluation if the tag's state has changed in a way that invalidates the prior suppression.",
      "aspects": []
    },
    {
      "id": "fork_detection_alerter",
      "node_type": "Subservice",
      "definition": "Identifies (chain, fork_id) candidates from pool replicas' chain-head observations: when replicas in a sub-pool diverge into two stable subgroups (neither a transient reorg), emits a fork-detected alert with a documented operator-decision SLA. Submission to region_coordinator's tip_quorum is gated on a local stability threshold: a (chain, fork_id) candidate is submitted only after at least K independent local replicas have confirmed it within a documented stability window, and only one canonical local-stability submission is sent per (chain, fork_id) per submission window — subsequent observations within the window update the local view but do not generate fresh submissions. Enforces an internal max-submissions-per-window rate limit documented to fit within tip_quorum's per-source rate budget with headroom; approaching the limit surfaces an alert; exceeding it suppresses further submissions in the window with a documented suppression count for operator audit. Each head observation submitted to tip_quorum is tagged with the authenticated source replica identity of the submitting chain_router replica and signed over the cert-bearing inter-region surface (mTLS to region_coordinator), so per-source rate limit and adversarial-source quarantine apply at tip_quorum. Each candidate carries the tip-freshness flag (read from pool_registry) and the freshness-derivation tag (hlc-affirmed or hlc-degraded) so tip_quorum's quorum can apply IR-stale-pool-quorum-exclusion and degradation-aware weighting at submission time.",
      "aspects": []
    },
    {
      "id": "pool_registry",
      "node_type": "StateSet",
      "definition": "Local in-process registry of every chain pool replica, sharded by (chain, fork_id): each sub-pool's lifecycle entries live in their own shard with independent CAS lines so high-rate transitions on one sub-pool do not contend with reads or writes on others. Each entry is keyed by (chain, fork_id, replica_id) and carries the replica's lifecycle state (admitted / draining / evicted / quarantined / deferred-quarantine), tip-freshness flag (with hlc-affirmed vs hlc-degraded derivation tag), current schema_version and canonical-bytes-version, replica-class (tip-serving / historical-archive / both), and the hlc_stamp of the last state transition. Sub-pool transitions carry a fork-transition hlc_stamp consumed by sub_pool_fork_partitioner's routing-table fence. Cross-shard reads (e.g. effective-capacity denominator, target-capacity audit) consume per-shard summaries rather than scanning the global registry. Lifecycle transitions are gated by drain_state_log per the membership manager's CAS rules; quarantine transitions respect the safe-membership floor.",
      "aspects": []
    },
    {
      "id": "quarantine_set",
      "node_type": "StateSet",
      "definition": "Per-replica quarantine evidence keyed by replica_id with the divergence reason (replica-skew / canonicalizer-fault / tip-divergence / fork-suspect), the observation evidence (canonical shape vs observed, canonical-bytes-version), the cohort the divergence was scored against (chain, method, schema_version, canonical-bytes-version), the rotation-in-progress tag's event-type and hlc_stamp at observation time, the suppression deadline, and the deferred-quarantine commit cap window state. Read by pool_membership_manager and operator surfaces; written only by schema_skew_quarantine and tip_freshness_tracker.",
      "aspects": []
    },
    {
      "id": "drain_state_log",
      "node_type": "StateSet",
      "definition": "Append-only log of drain transitions per replica keyed by drain_id and (chain, fork_id, replica_id): drain-requested, drain-in-progress, drain-completed, drain-aborted, force-completed (with M-of-N operator attribution), drain-stuck (with rebind-ack-missed reason). Each entry carries the gateway-rebind acknowledgement timestamp (when received), the authenticated signature and per-drain nonce of the ack, the lifecycle_gate gating evidence, and the bounded-wait deadline. Read by drain_coordinator and pool_membership_manager (whose lifecycle CAS rules consult the latest drain_state_log entry); written only by drain_coordinator.",
      "aspects": []
    }
  ],
  "edges": [
    [
      "sub_pool_fork_partitioner",
      "pool_membership_manager",
      {
        "edge_type": "calls"
      }
    ],
    [
      "pool_membership_manager",
      "response_canonicalizer",
      {
        "edge_type": "calls"
      }
    ],
    [
      "drain_coordinator",
      "pool_membership_manager",
      {
        "edge_type": "calls"
      }
    ],
    [
      "response_canonicalizer",
      "schema_skew_quarantine",
      {
        "edge_type": "calls"
      }
    ],
    [
      "tip_freshness_tracker",
      "fork_detection_alerter",
      {
        "edge_type": "calls"
      }
    ],
    [
      "sub_pool_fork_partitioner",
      "pool_registry",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "pool_membership_manager",
      "pool_registry",
      {
        "edge_type": "writes_to"
      }
    ],
    [
      "pool_membership_manager",
      "pool_registry",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "drain_coordinator",
      "drain_state_log",
      {
        "edge_type": "writes_to"
      }
    ],
    [
      "drain_coordinator",
      "drain_state_log",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "drain_coordinator",
      "pool_registry",
      {
        "edge_type": "writes_to"
      }
    ],
    [
      "schema_skew_quarantine",
      "quarantine_set",
      {
        "edge_type": "writes_to"
      }
    ],
    [
      "schema_skew_quarantine",
      "quarantine_set",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "schema_skew_quarantine",
      "pool_registry",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "tip_freshness_tracker",
      "pool_registry",
      {
        "edge_type": "writes_to"
      }
    ],
    [
      "tip_freshness_tracker",
      "pool_registry",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "fork_detection_alerter",
      "pool_registry",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "response_canonicalizer",
      "pool_registry",
      {
        "edge_type": "reads_from"
      }
    ],
    [
      "pool_membership_manager",
      "quarantine_set",
      {
        "edge_type": "reads_from"
      }
    ]
  ],
  "requirements": [
    {
      "id": "IR-fork-sub-pool-partition",
      "targets": [
        {
          "Node": "sub_pool_fork_partitioner"
        },
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "Each chain pool is partitioned by (chain_id, fork_id) so per-fork sub-pools serve fork-specific traffic; routing of an incoming RPC tagged with (chain, fork) goes only to the matching sub-pool, and replicas across forks are sub-pool members rather than divergent peers.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-pool-drain-protocol",
      "targets": [
        {
          "Node": "drain_coordinator"
        },
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "drain_state_log"
        }
      ],
      "description": "A replica entering 'draining' state stops receiving new requests, finishes in-flight RPCs (or returns them with a retryable-on-other-replica hint), and only after gateway has re-bound dependent subscriptions does the replica unsubscribe from fanout's head streams; eviction commits only after gateway-rebind acknowledgement.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-response-canonicalization",
      "targets": [
        {
          "Node": "response_canonicalizer"
        }
      ],
      "description": "Every JSON-RPC response forwarded from a chain pool replica is canonicalized against a per-(chain, method, schema_version) schema before reaching gateway: undocumented optional fields stripped or sorted, types coerced, intra-response ordering rules applied so response shape is independent of which client implementation answered.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-schema-skew-quarantine",
      "targets": [
        {
          "Node": "schema_skew_quarantine"
        },
        {
          "Node": "quarantine_set"
        },
        {
          "Node": "pool_membership_manager"
        }
      ],
      "description": "When a replica's canonicalized response shape diverges from the (chain, method, schema_version) pool consensus shape, the replica is quarantined from the active sub-pool and the divergence evidence is surfaced to operators.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-rotation-aware-skew",
      "targets": [
        {
          "Node": "schema_skew_quarantine"
        },
        {
          "Node": "tip_freshness_tracker"
        }
      ],
      "description": "Schema-skew and tip-divergence quarantines consult region_coordinator's rotation-in-progress tag and suppress quarantines triggered solely by the documented rotation window so rotation noise does not look like a misbehaving replica.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-tip-freshness-budget",
      "targets": [
        {
          "Node": "tip_freshness_tracker"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "Each chain has a per-chain tip-freshness budget; chain_router measures pool tip lag against an out-of-band reference (peer-regional pools via region_coordinator, chain peer count, expected slot advance) and marks pools tip-stale when they exceed the budget.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-fork-detection-alert",
      "targets": [
        {
          "Node": "fork_detection_alerter"
        }
      ],
      "description": "Detection of an unannounced consensus split (replicas in a sub-pool diverging into two stable subgroups, neither a transient reorg) surfaces a 'fork-detected' alert with a documented operator-decision SLA before the split is exposed to tenants as a separate fork sub-pool.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-head-observation-authenticated",
      "targets": [
        {
          "Node": "fork_detection_alerter"
        }
      ],
      "description": "Every head observation submitted to region_coordinator's tip_quorum is tagged with the authenticated source replica identity of the submitting chain_router replica and signed over the cert-bearing inter-region surface (mTLS to region_coordinator) so per-source rate limit and adversarial-source quarantine can be applied at tip_quorum.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "IR-offboarding-idempotency",
      "targets": [
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "drain_coordinator"
        }
      ],
      "description": "Cancellation of in-flight long-RPCs for offboarded tenants dedupes inbound offboarding signals from region_coordinator by idempotency key (offboarding_id, component_id, attempt_id), meets a documented attestation SLO, and surfaces preservation-blocked terminal states (e.g. when an in-flight long-RPC overlaps a preservation hold) so region_coordinator can record best-effort attestations rather than waiting indefinitely.",
      "origin": {
        "type": "Initial"
      }
    },
    {
      "id": "SR-canonical-bytes-spec",
      "targets": [
        {
          "Node": "response_canonicalizer"
        }
      ],
      "description": "response_canonicalizer's canonical output for a given (chain, method, schema_version) is defined by an explicit canonical-bytes spec (canonical-bytes-version, distinct from schema_version) that documents byte-level field ordering, numeric type representation, and whitespace policy. The canonicalizer refuses to emit bytes for any (chain, method, schema_version) lacking a registered canonical-bytes-version; encountering a previously-unseen field surface produces an explicit unknown-field signal rather than a silent canonicalization.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s1-canonicalizer-silent-drift"
      }
    },
    {
      "id": "SR-replica-fork-revalidate",
      "targets": [
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "On dispatch to a replica, pool_membership_manager re-validates the replica's currently-recorded fork_id in pool_registry against the request's (chain, fork_id) tag at the moment of dispatch (not just at routing-table read time); a mismatch returns a retryable-on-other-replica signal rather than answering with cross-fork data, so a fork transition cannot answer an old-fork request with new-fork data.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s2-fork-transition-misroute"
      }
    },
    {
      "id": "SR-fork-transition-routing-fence",
      "targets": [
        {
          "Node": "sub_pool_fork_partitioner"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "Sub-pool fork transitions in pool_registry are stamped with an hlc_stamp; sub_pool_fork_partitioner's routing table carries the hlc_stamp at which it was rebuilt and refuses to dispatch requests whose hlc_stamp would predate a transition affecting the target sub-pool — instead the request is bounced to be re-routed against the latest table.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s2-fork-transition-misroute"
      }
    },
    {
      "id": "SR-drain-state-cas-gate",
      "targets": [
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "drain_coordinator"
        },
        {
          "Node": "pool_registry"
        },
        {
          "Node": "drain_state_log"
        }
      ],
      "description": "pool_registry lifecycle transitions for a replica are gated on the replica's current drain_state_log entry: a transition to admitted requires drain-completed-or-aborted, a transition to evicted requires drain-completed; concurrent proposals are CAS-ordered through pool_registry so admit-during-drain or evict-after-readmit cannot both commit.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s3-drain-readd-race"
      }
    },
    {
      "id": "SR-drain-abort-explicit",
      "targets": [
        {
          "Node": "drain_coordinator"
        },
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "drain_state_log"
        }
      ],
      "description": "Re-admission of a draining replica requires an explicit drain-abort transition in drain_state_log before the admit can commit; pool_membership_manager cannot auto-admit a replica whose drain_state_log entry is drain-in-progress without first proposing drain-abort, which surfaces an alert and records the operator/system attribution.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s3-drain-readd-race"
      }
    },
    {
      "id": "SR-quarantine-schema-version-aware",
      "targets": [
        {
          "Node": "schema_skew_quarantine"
        },
        {
          "Node": "quarantine_set"
        }
      ],
      "description": "schema_skew_quarantine indexes pool consensus shape per (chain, method, schema_version) and only quarantines on divergence within the same schema_version cohort; a replica emitting shape under a new schema_version is compared against the new schema_version's cohort, not the old. A new-schema cohort is bootstrapped from the registered canonical-bytes-version spec rather than from existing pool members so divergence detection works even when only one replica has rolled to the new version.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s4-quarantine-cascade-rollout"
      }
    },
    {
      "id": "SR-quarantine-mass-evict-cap",
      "targets": [
        {
          "Node": "schema_skew_quarantine"
        },
        {
          "Node": "quarantine_set"
        },
        {
          "Node": "pool_membership_manager"
        }
      ],
      "description": "schema_skew_quarantine enforces a per-(chain, fork_id) max-concurrent-quarantine-commits cap within a documented window; quarantine commits beyond the cap are deferred and surfaced as an alert listing the deferred replicas. The cap is at most a documented fraction of sub-pool membership so quarantine cannot push a sub-pool below the safe-membership floor in a single window.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s4-quarantine-cascade-rollout"
      }
    },
    {
      "id": "SR-quarantine-floor-gate",
      "targets": [
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "Before a quarantine eviction commits in pool_registry via pool_membership_manager, the manager refuses the transition if it would push the (chain, fork_id) sub-pool's healthy-replica count below the documented safe-membership floor; refusal surfaces an alert with the (chain, fork_id) at risk and the candidate replica.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s4-quarantine-cascade-rollout"
      }
    },
    {
      "id": "SR-tip-freshness-hlc-degraded-freeze",
      "targets": [
        {
          "Node": "tip_freshness_tracker"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "On region_coordinator's HLC-degraded marker, tip_freshness_tracker freezes the tip-stale flag set in pool_registry: no transitions in or out of tip-stale commit while degraded; existing flags remain conservatively in place; the tracker surfaces a tip-freshness-suspended alert and refuses fallback to local wall-clock as the freshness reference.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s5-tip-freshness-clock-fallback"
      }
    },
    {
      "id": "SR-tip-freshness-chain-derived-fallback",
      "targets": [
        {
          "Node": "tip_freshness_tracker"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "When HLC is degraded, tip_freshness_tracker may use chain-derived signals (chain peer count, expected slot advance for the chain, peer-regional pool's chain-coord head delta as relayed by region_coordinator on heal) as freshness inputs because they are timestamp-free; explicit refusal to derive freshness from local wall-clock is documented. Any freshness flag derived from chain-only signals is tagged hlc-degraded so consumers (region_coordinator's tip_quorum) can apply it more conservatively.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s5-tip-freshness-clock-fallback"
      }
    },
    {
      "id": "SR-fork-alerter-local-stability-gate",
      "targets": [
        {
          "Node": "fork_detection_alerter"
        }
      ],
      "description": "fork_detection_alerter's submission to region_coordinator's tip_quorum is gated on a local stability threshold: a (chain, fork_id) candidate is submitted only after at least K independent local replicas have confirmed it within a documented stability window, and only one canonical local-stability submission is sent per (chain, fork_id) per submission window; subsequent observations within the window update the local view but do not generate fresh submissions.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s6-fork-alerter-flood"
      }
    },
    {
      "id": "SR-fork-alerter-self-rate-limit",
      "targets": [
        {
          "Node": "fork_detection_alerter"
        }
      ],
      "description": "fork_detection_alerter enforces an internal max-submissions-per-window rate limit that is documented to fit within tip_quorum's per-source rate budget with headroom; approaching the limit surfaces an alert; exceeding it suppresses further submissions in the window with a documented suppression count for operator audit.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s6-fork-alerter-flood"
      }
    },
    {
      "id": "SR-sub-pool-capacity-rebalance",
      "targets": [
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "pool_membership_manager exposes a documented per-(chain, fork_id) sub-pool target-capacity surface; when traffic share for a sub-pool exceeds its target by a documented margin, the manager proposes membership rebalances (admit additional replicas of that fork, drain over-provisioned rare-fork replicas) gated on the safe-membership floor for both the source and destination sub-pools. Rebalance proposals are committed via the same pool_registry CAS path as drain/admit and are visible in the audit.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s7-sub-pool-capacity-starvation"
      }
    },
    {
      "id": "SR-effective-capacity-denominator",
      "targets": [
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "sub_pool_fork_partitioner"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "pool_membership_manager's request-admission and per-replica load computations use the effective-capacity denominator (count of replicas currently admitted AND not draining AND not in deferred-quarantine AND not tip-stale) rather than total pool_registry membership; the denominator is recomputed on every relevant pool_registry transition and propagated to sub_pool_fork_partitioner's routing decisions so per-replica fan-out reflects the actually-serving subset.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s8-partial-quarantine-latency-amp"
      }
    },
    {
      "id": "SR-canonicalizer-quarantine-independent-validators",
      "targets": [
        {
          "Node": "response_canonicalizer"
        },
        {
          "Node": "schema_skew_quarantine"
        }
      ],
      "description": "response_canonicalizer's canonical-bytes output is validated against the canonical-bytes-version spec by an independent validator path that does not share code or in-process state with the canonicalizer's transform path; schema_skew_quarantine's divergence-detection input is the validator's verdict, not the canonicalizer's emission. A mis-transform produced by the canonicalizer is detected by the validator (and surfaced as a canonicalizer-fault alert distinct from a replica-skew alert) rather than feeding the divergence-detection path with mis-shaped 'consensus'.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s9-canonicalizer-quarantine-coupling"
      }
    },
    {
      "id": "SR-quarantine-canonicalizer-fault-distinct",
      "targets": [
        {
          "Node": "schema_skew_quarantine"
        },
        {
          "Node": "quarantine_set"
        }
      ],
      "description": "schema_skew_quarantine distinguishes a 'replica-skew' verdict (canonicalized bytes diverge from validator-affirmed pool consensus) from a 'canonicalizer-fault' verdict (validator rejects canonicalizer output as non-canonical) and refuses to commit replica quarantines when the prevailing fault is a canonicalizer-fault rather than a replica-skew, surfacing canonicalizer-fault as its own alert for separate operator response.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s9-canonicalizer-quarantine-coupling"
      }
    },
    {
      "id": "SR-pool-admit-override-mofn",
      "targets": [
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "pool_registry"
        }
      ],
      "description": "pool_membership_manager's operator-override admission path (used to admit replacement replicas faster than blue/green allows during emergency rollover) requires M-of-N operator quorum signatures (configurable threshold) carried with the admit proposal rather than a single operator credential; signing operators are recorded with the admit commit and visible in subsequent membership audit. The credential model and key custody are parent scope's responsibility (bubbled).",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s10-pool-admit-operator-override-credential"
      }
    },
    {
      "id": "SR-pool-admit-override-rate-limit",
      "targets": [
        {
          "Node": "pool_membership_manager"
        }
      ],
      "description": "pool_membership_manager enforces a documented max-overrides-per-window rate limit on operator-override admissions that bypass the safe-membership-floor; approaching the limit surfaces an alert; exceeding it rejects further override admits with a documented error code visible to the operator path.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s10-pool-admit-operator-override-credential"
      }
    },
    {
      "id": "SR-rotation-tag-hlc-stamped",
      "targets": [
        {
          "Node": "schema_skew_quarantine"
        },
        {
          "Node": "tip_freshness_tracker"
        }
      ],
      "description": "schema_skew_quarantine and tip_freshness_tracker stamp every quarantine/transition proposal with the rotation-tag's hlc_stamp at observation time; a candidate is suppressed only if the rotation tag was active at the observation hlc_stamp (not at decision time). On commit, the proposal's stamped tag is re-checked against current region_coordinator state; if the tag has since changed and the change invalidates the suppression decision, the proposal is re-evaluated rather than committed under stale assumptions.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s11-rotation-tag-stale-read"
      }
    },
    {
      "id": "SR-drain-rebind-ack-bounded",
      "targets": [
        {
          "Node": "drain_coordinator"
        },
        {
          "Node": "drain_state_log"
        }
      ],
      "description": "drain_coordinator enforces a bounded drain-completion wait: if gateway-rebind ack is not received within a documented window, drain_coordinator escalates by surfacing a drain-stuck alert and either retries the rebind request (idempotent) or proposes a force-complete transition that is itself gated on operator override (M-of-N-signed) so a stuck gateway cannot indefinitely hold drain capacity hostage. The stuck-drain event is recorded in drain_state_log with the rebind-ack-missed reason.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s12-drain-rebind-ack-forgery-or-stall"
      }
    },
    {
      "id": "SR-drain-rebind-ack-authenticated",
      "targets": [
        {
          "Node": "drain_coordinator"
        },
        {
          "Node": "drain_state_log"
        }
      ],
      "description": "Gateway-rebind acknowledgements consumed by drain_coordinator carry an authenticated signature over the cert-bearing gateway-to-chain_router channel and a per-drain nonce (the drain_id from drain_state_log), so a replayed ack from a prior drain or a forged ack cannot complete an active drain.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s12-drain-rebind-ack-forgery-or-stall"
      }
    },
    {
      "id": "SR-pool-registry-shard-by-fork",
      "targets": [
        {
          "Node": "pool_registry"
        },
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "tip_freshness_tracker"
        }
      ],
      "description": "pool_registry is sharded by (chain, fork_id): each sub-pool's lifecycle entries live in their own shard with independent CAS lines so high-rate transitions on one sub-pool do not contend with reads or writes on others. Cross-shard reads (e.g. effective-capacity denominator) consume shard-level summaries rather than scanning the global registry.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s13-pool-registry-hot-shard"
      }
    },
    {
      "id": "SR-replica-class-tip-vs-archive",
      "targets": [
        {
          "Node": "pool_registry"
        },
        {
          "Node": "pool_membership_manager"
        },
        {
          "Node": "sub_pool_fork_partitioner"
        }
      ],
      "description": "pool_registry records each replica's replica-class (tip-serving / historical-archive / both) per (chain, fork_id, replica_id); sub_pool_fork_partitioner uses the cost-class hint of the inbound RPC (short-RPC tip-sensitive vs long-RPC archive) to route only to a replica whose class includes the request's class. pool_membership_manager refuses to admit a tip-class request to an archive-only replica and vice versa, so heavy archive queries cannot saturate tip-serving capacity.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s14-historical-vs-tip-isolation"
      }
    },
    {
      "id": "SR-rotation-tag-typed",
      "targets": [
        {
          "Node": "schema_skew_quarantine"
        },
        {
          "Node": "tip_freshness_tracker"
        }
      ],
      "description": "region_coordinator's rotation-in-progress tag is consumed by chain_router with explicit event-type discrimination: schema_skew_quarantine suppresses only on chain-pool blue/green and chain-client-version rotation events (which legitimately change response shape); tip_freshness_tracker suppresses only on chain-pool blue/green rotation events; cert and origin-endpoint rotations do not suppress either quarantine path. The tag's event-type is consumed at observation time and stamped on the proposal alongside SR-rotation-tag-hlc-stamped.",
      "origin": {
        "type": "Stressor",
        "session": 1,
        "stressor": "s15-rotation-tag-orthogonal-events"
      }
    }
  ],
  "satisfactions": [
    {
      "requirement_id": "IR-fork-sub-pool-partition",
      "explanation": "sub_pool_fork_partitioner reads (chain, fork) tag and routes only to the matching sub-pool; pool_membership_manager keys per-sub-pool membership in pool_registry by (chain, fork_id, replica_id); requests for unknown fork_id are refused rather than falling through to a default."
    },
    {
      "requirement_id": "IR-pool-drain-protocol",
      "explanation": "drain_coordinator commits draining state in pool_registry, refuses new requests, holds in-flight or returns retryable-on-other-replica, waits for gateway-rebind acknowledgement (recorded in drain_state_log), then commits eviction; pool_membership_manager honours the draining flag in routing."
    },
    {
      "requirement_id": "IR-response-canonicalization",
      "explanation": "response_canonicalizer normalizes each response against per-(chain, method, schema_version) schema (strip/sort optional fields, coerce types, ordering rules) before forwarding to gateway."
    },
    {
      "requirement_id": "IR-schema-skew-quarantine",
      "explanation": "schema_skew_quarantine compares canonicalized response shape against pool consensus shape, writes divergence evidence into quarantine_set, and proposes the replica's transition to quarantined in pool_registry via pool_membership_manager."
    },
    {
      "requirement_id": "IR-rotation-aware-skew",
      "explanation": "schema_skew_quarantine and tip_freshness_tracker tag candidate quarantines with the rotation-in-progress tag at observation time and suppress quarantine commit while the tag holds, consistent with the parent contract."
    },
    {
      "requirement_id": "IR-tip-freshness-budget",
      "explanation": "tip_freshness_tracker measures pool tip lag against the out-of-band reference (peer-regional pools via region_coordinator, chain peer count, expected slot advance) and writes tip-stale flags into pool_registry per replica when the per-chain budget is exceeded."
    },
    {
      "requirement_id": "IR-fork-detection-alert",
      "explanation": "fork_detection_alerter identifies stable two-subgroup divergence within a sub-pool (not transient reorgs) and emits a fork-detected alert with operator-decision SLA before exposing the split as a separate sub-pool."
    },
    {
      "requirement_id": "IR-head-observation-authenticated",
      "explanation": "fork_detection_alerter submits every head observation to region_coordinator's tip_quorum tagged with the authenticated source replica identity and signed over the cert-bearing inter-region surface; per-source rate limit applies at submission."
    },
    {
      "requirement_id": "IR-offboarding-idempotency",
      "explanation": "pool_membership_manager and drain_coordinator dedupe inbound offboarding signals by (offboarding_id, component_id, attempt_id), meet a documented attestation SLO, and surface preservation-blocked terminal states when an in-flight long-RPC overlaps a preservation hold."
    },
    {
      "requirement_id": "SR-canonical-bytes-spec",
      "explanation": "response_canonicalizer redefinition: 'output is defined by an explicit canonical-bytes spec keyed by (chain, method, schema_version, canonical-bytes-version); refuses to emit bytes for any tuple lacking a registered canonical-bytes-version; previously-unseen field surface produces an explicit unknown-field signal rather than silent canonicalization'."
    },
    {
      "requirement_id": "SR-replica-fork-revalidate",
      "explanation": "pool_membership_manager redefinition: 'on dispatch the manager re-validates the replica's currently-recorded fork_id against the request's (chain, fork_id) tag at the moment of dispatch (not just at routing-table read time); a mismatch returns retryable-on-other-replica'."
    },
    {
      "requirement_id": "SR-fork-transition-routing-fence",
      "explanation": "sub_pool_fork_partitioner redefinition: 'routing table carries the hlc_stamp at which it was rebuilt and refuses to dispatch requests whose lookup pre-dates a fork-transition stamp affecting the target sub-pool — bounced for re-routing'. pool_registry redefinition: 'sub-pool transitions carry a fork-transition hlc_stamp'."
    },
    {
      "requirement_id": "SR-drain-state-cas-gate",
      "explanation": "pool_membership_manager redefinition: 'lifecycle transitions in pool_registry are gated on the replica's current drain_state_log entry: admit requires drain-completed-or-aborted, evict requires drain-completed; concurrent proposals are CAS-ordered through pool_registry per shard'. drain_coordinator redefinition: 'pool_registry transitions written by drain_coordinator participate in the same per-(chain, fork_id) shard CAS line that pool_membership_manager uses'."
    },
    {
      "requirement_id": "SR-drain-abort-explicit",
      "explanation": "pool_membership_manager redefinition: 're-admission of a draining replica requires an explicit drain-abort transition in drain_state_log before admit can commit; auto-admit cannot transition a replica with drain_state_log = drain-in-progress without first proposing drain-abort, surfacing an alert with attribution'."
    },
    {
      "requirement_id": "SR-quarantine-schema-version-aware",
      "explanation": "schema_skew_quarantine redefinition: 'divergence detection indexes consensus shape per cohort (chain, method, schema_version, canonical-bytes-version); a new cohort is bootstrapped from the registered canonical-bytes-version spec rather than from existing pool members so detection works even when only one replica has rolled to the new version'."
    },
    {
      "requirement_id": "SR-quarantine-mass-evict-cap",
      "explanation": "schema_skew_quarantine redefinition: 'enforces a per-(chain, fork_id) max-concurrent-quarantine-commits cap within a documented window; commits beyond the cap are deferred and surfaced as alerts; the cap is at most a documented fraction of sub-pool membership'."
    },
    {
      "requirement_id": "SR-quarantine-floor-gate",
      "explanation": "pool_membership_manager redefinition: 'refuses any quarantine eviction whose commit would push the (chain, fork_id) sub-pool's healthy-replica count below the documented safe-membership floor; refusal surfaces an alert with the (chain, fork_id) at risk'."
    },
    {
      "requirement_id": "SR-tip-freshness-hlc-degraded-freeze",
      "explanation": "tip_freshness_tracker redefinition: 'on the HLC-degraded marker the tracker freezes the tip-stale flag set in pool_registry — no transitions in or out of tip-stale commit while degraded, existing flags remain conservatively in place; refuses fallback to local wall-clock as the freshness reference'."
    },
    {
      "requirement_id": "SR-tip-freshness-chain-derived-fallback",
      "explanation": "tip_freshness_tracker redefinition: 'when HLC is degraded, the tracker may use chain-derived signals (chain peer count, expected slot advance, peer-regional pool's chain-coord head delta as relayed by region_coordinator on heal); flags derived from chain-only signals are tagged hlc-degraded so consumers apply them more conservatively'."
    },
    {
      "requirement_id": "SR-fork-alerter-local-stability-gate",
      "explanation": "fork_detection_alerter redefinition: 'submission gated on local stability — at least K independent local replicas confirm within a documented stability window; only one canonical submission per (chain, fork_id) per window'."
    },
    {
      "requirement_id": "SR-fork-alerter-self-rate-limit",
      "explanation": "fork_detection_alerter redefinition: 'enforces an internal max-submissions-per-window rate limit documented to fit within tip_quorum's per-source rate budget with headroom; approaching the limit surfaces an alert; exceeding it suppresses further submissions with documented suppression count'."
    },
    {
      "requirement_id": "SR-sub-pool-capacity-rebalance",
      "explanation": "pool_membership_manager redefinition: 'exposes a per-(chain, fork_id) target-capacity surface; when traffic share exceeds target by a documented margin, proposes membership rebalances gated on safe-membership floor for both source and destination sub-pools, committed via the same CAS path and visible in audit'."
    },
    {
      "requirement_id": "SR-effective-capacity-denominator",
      "explanation": "pool_membership_manager redefinition: 'computes the effective-capacity denominator (admitted AND not draining AND not in deferred-quarantine AND not tip-stale) from per-shard summaries on every relevant transition and propagates it to sub_pool_fork_partitioner'. sub_pool_fork_partitioner redefinition: 'per-replica fan-out within a sub-pool uses the effective-capacity denominator propagated from pool_membership_manager rather than raw pool_registry membership'."
    },
    {
      "requirement_id": "SR-canonicalizer-quarantine-independent-validators",
      "explanation": "response_canonicalizer redefinition: 'canonical-bytes output is validated against the canonical-bytes-version spec by an independent validator path that does not share code or in-process state with the transform path; the validator's verdict is the input that schema_skew_quarantine consumes'."
    },
    {
      "requirement_id": "SR-quarantine-canonicalizer-fault-distinct",
      "explanation": "schema_skew_quarantine redefinition: 'distinguishes a replica-skew verdict from a canonicalizer-fault verdict and refuses to commit replica quarantines when the prevailing fault is a canonicalizer-fault, surfacing canonicalizer-fault as its own alert'."
    },
    {
      "requirement_id": "SR-pool-admit-override-mofn",
      "explanation": "pool_membership_manager redefinition: 'operator-override admission requires M-of-N operator quorum signatures (configurable threshold) on the admit proposal — signing operators recorded with admit commit and visible in audit'."
    },
    {
      "requirement_id": "SR-pool-admit-override-rate-limit",
      "explanation": "pool_membership_manager redefinition: 'enforces a documented max-overrides-per-window rate limit on operator-override admissions that bypass the safe-membership floor; approaching the limit surfaces an alert; exceeding it rejects further override admits with a documented error code'."
    },
    {
      "requirement_id": "SR-rotation-tag-hlc-stamped",
      "explanation": "schema_skew_quarantine redefinition: 'rotation-tag's hlc_stamp is stamped on the proposal at observation, suppression decision tied to that stamp, commit-time re-check forces re-evaluation if the tag changed'. tip_freshness_tracker redefinition mirrors the same."
    },
    {
      "requirement_id": "SR-drain-rebind-ack-bounded",
      "explanation": "drain_coordinator redefinition: 'drain-completion gated on a bounded wait: if gateway-rebind ack not received within a documented window, surfaces drain-stuck alert and either retries idempotently or proposes a force-complete transition that requires M-of-N-signed operator override; recorded in drain_state_log'."
    },
    {
      "requirement_id": "SR-drain-rebind-ack-authenticated",
      "explanation": "drain_coordinator redefinition: 'gateway-rebind acknowledgements carry an authenticated signature over the cert-bearing gateway-to-chain_router channel and a per-drain nonce (the drain_id from drain_state_log) so a replayed or forged ack cannot complete an active drain'. drain_state_log redefinition: 'each entry carries the authenticated signature and per-drain nonce of the ack'."
    },
    {
      "requirement_id": "SR-pool-registry-shard-by-fork",
      "explanation": "pool_registry redefinition: 'sharded by (chain, fork_id): each sub-pool's lifecycle entries live in their own shard with independent CAS lines so high-rate transitions on one sub-pool do not contend with reads or writes on others; cross-shard reads consume per-shard summaries'."
    },
    {
      "requirement_id": "SR-replica-class-tip-vs-archive",
      "explanation": "pool_registry redefinition: 'replica-class (tip-serving / historical-archive / both) recorded per replica'. pool_membership_manager redefinition: 'records per-replica replica-class; refuses to admit a tip-class request to an archive-only replica and vice versa'. sub_pool_fork_partitioner redefinition: 'consumes the request's cost-class hint and routes only to a replica whose replica-class includes the request's class'."
    },
    {
      "requirement_id": "SR-rotation-tag-typed",
      "explanation": "schema_skew_quarantine redefinition: 'suppression only when rotation-tag at observation time was active AND rotation event-type is one that can legitimately change response shape (chain-pool blue/green or chain-client-version, not cert or origin-endpoint)'. tip_freshness_tracker redefinition: 'suppressing only on chain-pool blue/green rotation events, not cert/origin-endpoint or chain-client-version alone'."
    }
  ]
}